{{include('partials/header.twig')}}

<h1>test</h1>
<div class="col-lg-12">
{{ form_start(form,{attr:{class:'navbar-form navbar-left'}}) }}
	{{ form_errors(form) }} 
	    <div class="form-group">
	        {{ form_widget(form.title,{attr:{class:'form-control', placeholder:'titre'}}) }}
	        {{ form_errors(form.title) }}
	    </div>
	    <div class="form-group">
	        {{ form_widget(form.preview,{attr:{class:'form-control', placeholder:'short description'}}) }}
	        {{ form_errors(form.preview) }}
	    </div>
	    <div class="form-group">
	        {{ form_widget(form.picture,{attr:{class:'form-control', placeholder:'url image'}}) }}
	        {{ form_errors(form.picture) }}
	    </div>
	    <div class="form-group">
	        {{ form_widget(form.slug,{attr:{class:'form-control', placeholder:'slug'}}) }}
	        {{ form_errors(form.slug) }}
	    </div>
	    <div class="form-group">
	        {{ form_widget(form.content,{attr:{class:'form-control', placeholder:'contenu'}}) }}
	        {{ form_errors(form.content) }}
	    </div>
	        {{ form_widget(form.submit, {attr: {class:'btn btn-default'}}) }}
	{{ form_end(form) }}
</div>

{{include('partials/footer.twig')}}